import 'package:flutter/material.dart';

class DetailSymptom extends StatelessWidget {
  const DetailSymptom({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }

}